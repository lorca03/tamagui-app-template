{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.5.0 --activate && yarn install",
  "buildCommand": "yarn workspace next-app build",
  "outputDirectory": "apps/next/.next"
}
